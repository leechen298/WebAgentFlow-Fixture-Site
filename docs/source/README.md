# Source Planning Documents

This directory contains historical planning and review documents copied from
`leechen298/WebAgentFlow` at commit `02c56e7e38208c8400391f882a07e7ef4aed534f`.

These files are preserved as source context. Paths inside these documents may
refer to the original WebAgentFlow monorepo layout.

Current repository layout:

- old `apps/validation-site` -> `web/`
- old `apps/api/app/routers/validation_api.py` -> `server/app/validation_api.py`

`11.3.3-product-chat-test-site-separation` is historical boundary context only.
It does not mean this repository contains or should contain `apps/product-test-site`.
